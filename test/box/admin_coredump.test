# encoding: utf-8
# 
import sys
# moved to separete test becouse this test fail in the valgrind mode (coredump
# process terminated via abort procedure).
admin("box.save_coredump()")
# vim: syntax=python
